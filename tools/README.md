# Tools

Small development and analysis helpers used with PicoTracker telemetry data are kept here.

- `decode_data.py` contains the historical telemetry decoding/checksum helper.
- `with_pips_data.txt` is retained as sample captured data for development and inspection.

These utilities are historical project tooling and are separate from the embedded tracker firmware.
